import { RainbowWriterDirective } from './rainbow-writer.directive';

describe('RainbowWriterDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowWriterDirective();
    expect(directive).toBeTruthy();
  });
});
